{
  "name": "store-scrap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs --incremental",
    "build:full": "node scripts/build.mjs --full",
    "lint": "node -c scripts/build.mjs"
  },
  "dependencies": {
    "google-play-scraper": "^9.0.2"
  }
}
